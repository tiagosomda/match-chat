while the app is generic matches, things should be grouped on a per tournament basis. However, for now, in our first iteration, we will only target the world cup 2026, so we don't need to have a landing page where we need to pick which tournaments to see the list of matches from, we should load directtly into the world cup - however, the app itself shouldn't know about it as everything should be generic in the front end... tournaments have matches and all of that data is retrieved from the backend. so I guess the player profile/settings should have a setting saying which tournament to load into (if there is no prefered one to load into or a last loaded tournament that the app was in before the app closed.
